import 'package:flutter/material.dart';

class TaxPage extends StatelessWidget {
  const TaxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Module Fiscalité - À implémenter"));
  }
}
